# Open Website Example Plugin（示例）

这是一个更直观的网络能力示例。
它把两件事拆开演示：

- 用 `openUrl` 直接打开 `https://www.bilibili.com/`
- 在页面事件里调用 `ctx.net.request()`
- 处理请求成功和失败状态，并返回 `toast`

如果你想先看最小页面和 slot，请先看 `plugins/example.quickstart/1.0.0/`。
