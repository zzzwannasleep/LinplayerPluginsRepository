# PC Demo Plugin（示例）

这个插件只面向 `pc`，用于演示：
- 更偏信息密度的双栏布局（`row` + `column`）
- `contributions.slots` 在首页/详情页/播放页的注入点（安装后更容易看到变化）
- `contributions.pages`：在「插件中心」打开插件自有页面（示例页）
- `pages.entry=true`：在首页顶栏（`主页` / `喜欢`）旁显示「示例」入口（需要宿主实现）

入口脚本：`main.js`  
清单：`manifest.json`
