# PC Demo Plugin（示例）

这个插件只面向 `pc`，用于演示：
- 更偏信息密度的双栏布局（`row` + `column`）
- `contributions.slots` 在首页/详情页/播放页的注入点（安装后更容易看到变化）
- `pages.entry=true` 在 PC 顶栏一级入口的展示（宿主可选实现，用于在「首页 / 喜欢」旁加入「示例」）

入口脚本：`main.js`  
清单：`manifest.json`
